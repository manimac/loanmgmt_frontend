import { FilterLoansPipe } from './filter-loans.pipe';

describe('FilterLoansPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLoansPipe();
    expect(pipe).toBeTruthy();
  });
});
